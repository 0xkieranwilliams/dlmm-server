# Solana RPC URL (optional, defaults to public mainnet endpoint)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Server port (optional, defaults to 3000)
PORT=3000

# Path to Solana keypair JSON file (for scripts that need signing authority)
KEYPAIR_PATH=./id.json